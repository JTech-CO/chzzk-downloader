# Chzzk Downloader v2.2.6 업데이트 안내

안녕하세요, 치지직 다운로더 개발자입니다.

이번 **v2.2.6**은 직접 MP4로 제공되는 VOD의 단일 연결 다운로드 병목을 개선한 속도 업데이트입니다. v2.2.5의 전체 파일은 `legacy/v2.2.5/`에 보존했으며, HLS/fMP4 재생 호환성 보정 경로는 변경하지 않았습니다.

---

## 1. VOD 다운로드 속도가 느렸던 원인

`inKey`가 있는 정식 변환 VOD는 최고 화질의 완성된 MP4 URL을 받을 수 있습니다. v2.2.5까지는 이 URL을 `chrome.downloads`에 한 번 전달해 브라우저가 단일 연결로 내려받았습니다.

확장 프로그램 자체가 원본 화질 MP4를 느리게 변환한 것은 아니지만, CDN과 사용자 회선 사이의 단일 연결 처리량이 낮은 경우 영상 1시간을 받는 데 1시간 이상 걸릴 수 있었습니다. 반면 여러 구간을 동시에 요청하면 같은 CDN에서도 사용 가능한 회선 대역폭을 더 잘 활용할 수 있습니다.

`inKey`가 없는 라이브 다시보기는 여전히 HLS/fMP4 조각을 병합하고 재생 메타데이터를 완성해야 하므로, 직접 MP4 VOD와 처리 과정이 다릅니다.

---

## 2. 병렬 HTTP Range 다운로드 추가

256 MiB 이상의 직접 MP4에 다음 절차를 적용합니다.

1. `bytes=0-0` 사전 요청으로 `206 Partial Content` 지원 여부와 전체 파일 크기를 확인합니다.
2. 원본 MP4를 16 MiB의 연속된 Range로 나눕니다.
3. 최대 8개 구간을 동시에 내려받습니다.
4. 완료 순서와 관계없이 OPFS 임시 파일에 원래 바이트 순서대로 기록합니다.
5. 기록된 총 바이트 수가 사전 확인한 원본 크기와 같을 때만 사용자 저장 단계로 이동합니다.

기록 위치보다 최대 8개 조각만 앞서가도록 백프레셔를 적용했습니다. 따라서 10~12시간급 영상도 파일 전체를 메모리에 쌓지 않으며, 영상 길이를 이유로 다운로드를 취소하지 않습니다.

속도 향상 폭은 CDN 상태, 사용자 회선, 저장 장치 성능에 따라 달라질 수 있습니다.

---

## 3. 화질과 플레이어 호환성

직접 MP4 경로는 영상을 재인코딩하거나 MP4 컨테이너를 다시 만들지 않습니다.

- 원본의 `ftyp`, `moov`, `mdat` 및 탐색 메타데이터를 수정하지 않음
- 각 Range의 바이트를 정확한 원래 위치에 그대로 기록
- 최종 크기가 서버 원본과 같은지 검증
- 화질, 코덱, 프레임, 오디오 데이터를 변경하지 않음

따라서 병렬 방식은 전송 방법만 바꾸며, 완성 파일의 팟플레이어·Windows Media Player·macOS 플레이어 호환성은 서버가 제공한 원본 MP4와 같습니다.

`inKey` 없는 HLS/fMP4에는 v2.2.5의 duration 보정, A/V 타임라인 정규화, 64비트 `mfra` 탐색 인덱스 생성이 그대로 적용됩니다. 직접 MP4 가속을 위해 이 호환성 로직을 우회하거나 제거하지 않았습니다.

---

## 4. 오류 및 손상 방지

각 병렬 응답에 다음 검증을 적용합니다.

- HTTP 상태가 정확히 `206`인지 확인
- 요청한 시작·끝 위치와 `Content-Range`가 일치하는지 확인
- 모든 응답의 전체 파일 크기가 같은지 확인
- ETag 또는 Last-Modified가 사전 확인 값과 충돌하지 않는지 확인
- 응답 본문 크기가 요청한 Range와 정확히 같은지 확인
- 완성 파일의 총 바이트 수가 원본 크기와 같은지 확인

`429` 또는 일시적인 서버 오류는 백오프 후 최대 4회 재시도합니다. 서버가 Range를 무시하고 전체 파일(`200`)을 보내면 본문을 즉시 취소해 중복 대용량 응답을 막습니다.

Range 미지원, 256 MiB 미만, OPFS 미지원 환경에서는 다운로드 시작 전에 기존 브라우저 단일 다운로드로 자동 복귀합니다. 병렬 기록이 시작된 뒤 검증에 실패하면 불완전한 임시 파일을 삭제하고 오류를 표시하며, 손상 가능성이 있는 파일을 저장하지 않습니다.

---

## 5. 검증 결과

자동 단위 테스트 11개로 다음 동작을 확인했습니다.

- Range 계획의 연속성, 무중복, 전체 크기 일치
- 사전 요청의 전체 크기 및 강한 ETag 인식
- Range 미지원 전체 응답의 즉시 취소
- `Content-Range` 및 파일 식별자 변경 탐지
- `503` 이후 재시도 성공
- 요청보다 큰 응답 본문 차단
- 지정된 워커 동시성 상한 준수

공개 치지직 VOD CDN 통합 확인에서는 모든 요청이 `206`을 반환했고, 인접한 두 Range를 이어 붙인 128 KiB 샘플이 같은 구간을 한 번에 받은 결과와 바이트 및 SHA-256 값까지 일치했습니다.

---

## 6. 유지되는 보호 장치

- 허용된 치지직 미디어 도메인과 HTTPS만 접근
- 로컬호스트·사설망 주소 차단
- 치지직 탭에서 보낸 메시지만 처리
- HLS 외부 CDN은 지정 호스트의 `/chzzk/` 경로로 제한
- 대용량 파일은 OPFS 디스크 스트리밍
- 중단된 `cdl_*` 임시 파일 시작 시 정리
- 라이브 방송 화면에서는 다운로드 UI 숨김

새 권한이나 개발자 서버는 추가하지 않았습니다.

---

## 업데이트 방법

기존 설치본을 **v2.2.6**으로 업데이트해 주세요. 개발자 모드로 설치한 경우 최신 `chzzk-downloader.zip`의 압축을 해제한 뒤 확장 프로그램 관리 화면에서 다시 로드하면 됩니다.